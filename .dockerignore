__pycache__
*.pyc
*.pyo
.venv/
venv/
profiles/
output/
cache/
error_logs/
task_results/
local_storage.json
db.sqlite3
.env
.git/
env/
*.db
# Tests don't need to be in the image
tests/
.github/
.pytest_cache/
# Note: top-level data/ doesn't need to be in .dockerignore because the
# Dockerfile uses explicit COPY commands instead of `COPY . .`
