# Mapo — pinned dependencies

# Web framework
fastapi==0.136.1
uvicorn[standard]==0.46.0
pydantic==2.13.3
websockets==14.2

# Browser automation + anti-detection
camoufox[geoip]>=0.4.0               # Anti-detection Firefox (default)
patchright>=1.58.0                    # Anti-detection Chromium (fallback, ARM-native)
playwright>=1.49.0

# HTTP client
httpx[http2]==0.28.1

# Scraping + parsing
lxml==6.1.0
regex==2026.4.4
Unidecode==1.4.0
dateutils==0.6.12

# Config + utils
PyYAML==6.0.3
tenacity==9.1.4

# CLI + output
rich==14.3.3
openpyxl==3.1.5

# Scheduling + storage
APScheduler==3.11.2
aiosqlite==0.22.1

# Transitive deps — pinned
anyio==4.13.0
certifi==2026.4.22
et_xmlfile==2.0.0
h11==0.16.0
h2==4.3.0
hpack==4.1.0
httpcore==1.0.9
hyperframe==6.1.0
idna==3.13
markdown-it-py==4.0.0
mdurl==0.1.2
Pygments==2.20.0
python-dateutil==2.9.0.post0
pytz==2026.1.post1
six==1.17.0
typing_extensions==4.15.0
tzlocal==5.3.1

# Optional — install as needed:
# psycopg2-binary==2.9.10
# gspread==6.1.4
# google-auth==2.38.0
# boto3==1.35.0
# anthropic==0.42.0
# openai==1.60.0
